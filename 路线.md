调研一下diffusion model 对没有标注的数据能不能做，我有一些没标准的图像数据可以试试

用生成模型生成函数型数据（也就是图像的方法）

# 关于数据

我的数据应该是：DICOM 格式的三维医学影像序列

往下滑动是身体的不同区域

重症肌无力。

DICOM（Digital Imaging and Communications in Medicine ) 即医学数字成像和通信，是医学图像和相关信息的国际标准（ISO 12052）。它定义了质量能满足临床需要的可用于数据交换的医学图像格式 。

一个患者的一次 CT 检查 = 一个 DICOM series
一个 DICOM series = 多个 .dcm 文件
每个 .dcm 文件 = 一张 CT 切片
多张连续切片叠起来 = 一个 3D CT 体积

需要每位重症肌无力患者一次或多次胸部 CT 检查的完整脱敏 DICOM series，包括平扫/增强信息、层厚、像素间距、扫描时间等元数据。

从2D到3D

如果你要研究胸腺区域，数据还应该包括什么？除了 DICOM，你最好还要：

1. 患者编号，脱敏后的 ID
2. 检查时间
3. CT 类型：平扫 / 增强
4. 层厚
5. 胸腺状态：正常 / 萎缩 / 胸腺增生 / 胸腺瘤
6. 是否病理证实胸腺瘤或胸腺增生
7. MG 诊断信息
8. 抗体状态：AChR、MuSK、LRP4 等
9. MGFA 分型或临床严重程度

本研究拟纳入重症肌无力患者完整胸部 CT DICOM series。首先对 DICOM 数据进行脱敏、质控和格式转换，将每位患者的一次 CT 检查转换为三维 NIfTI 体积数据；随后进行空间重采样、HU 值标准化和前纵隔/胸腺区域 ROI 裁剪，构建患者级 3D CT 数据集。模型训练采用患者级训练集、验证集和测试集划分，避免切片级数据泄漏。在模型构建方面，拟采用 3D 潜空间扩散模型，先训练或迁移医学 CT Autoencoder 以获得低维潜在表征，再在潜空间中训练条件扩散模型。条件变量包括胸腺状态、扫描类型及临床信息。推理阶段，模型根据给定条件生成三维 CT 体积，并保存为 NIfTI 或 DICOM series 以支持连续切片浏览。

完整胸部 DICOM series
↓
转换成 3D CT volume
↓
定位前纵隔 / 胸腺区域
↓
裁剪固定大小 ROI
↓
训练模型
